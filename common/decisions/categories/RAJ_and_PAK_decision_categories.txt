kashmir_influence_battle_category = {
	icon = decision_generic_nationalism

	allowed = {
		OR = {
			original_tag = RAJ
			original_tag = PAK
		}
		NOT = {
			PAK = {
				has_country_flag = pakistan_india_war	# this flag enabling makes the Battle for Kashmir category invisible (just one scope can be used to disable it for Pakistan & India)
			}
		}
	}

	visible = {
		OR = {
			original_tag = RAJ
			original_tag = PAK
		}
		NOT = {
			PAK = {
				has_country_flag = pakistan_india_war	# this flag enabling makes the Battle for Kashmir category invisible (just one scope can be used to disable it for Pakistan & India)
			}
		}
	}
}